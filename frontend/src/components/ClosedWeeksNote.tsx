export function ClosedWeeksNote() {
  return (
    <p className="max-w-[72ch] rounded border border-[#e3e1dc] border-l-4 border-l-[#0f4c5c] bg-white px-4 py-3 text-[13px] leading-relaxed text-[#4b504b] text-pretty">
      O painel consulta só semanas já encerradas (domingo a sábado). A semana
      vigente ainda está aberta e não entra nas três leituras — o boletim da
      Infodengue costuma ser publicado depois do fechamento.
    </p>
  );
}
