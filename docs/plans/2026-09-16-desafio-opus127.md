# Alertas de Dengue BH — Implementation Plan

**Goal:** Entregar Q1 (sync 6 meses AlertaDengue → SQL Server), Q2 (`GET` por `ew`/`ey`) e Q3 (SPA com três `GET`s), com testes no SQL real e README.

**Architecture:** `backend/` (solution .NET, `src/Api` com pastas Controller → Service → Repository, testes em `backend/tests`) e `frontend/` (Vite React TS Tailwind). EF Core, SQL Server no Compose da raiz. A interface consome só `GET /api/dengue`. Sync no hosted service + `POST /api/dengue/sync` (uma carga por vez), upsert por (ano, semana). Fuso `America/Sao_Paulo`. API em `http://localhost:5080` com Swagger. Git: `main` + uma branch por fatia. `.claude/` fora do Git.

Contratos e regras: [PRD](../prd.md). Decisões: [ADRs](../adr/).

---

### Task 1: Solution, Compose e esqueleto da API

**Files:**
- Create: `backend/Opus127.Dengue.sln`
- Create: `backend/src/Api/Opus127.Dengue.Api.csproj`
- Create: `backend/src/Api/Program.cs`
- Create: `backend/src/Api/appsettings.json`
- Create: `backend/src/Api/appsettings.Development.json`
- Create: `docker-compose.yml`
- Create: `backend/src/Api/Properties/launchSettings.json` (só `http://localhost:5080`)

- [x] SDK .NET 10.0.401 instalado na máquina.
- [x] `.gitignore` na raiz da `main` (inclui `.claude/`).
- [x] Na raiz do repo, criar a solution **dentro de `backend/`** e a API **com controllers** (não Minimal API como superfície):

```powershell
dotnet new sln -n Opus127.Dengue -o backend
dotnet new webapi -n Opus127.Dengue.Api -o backend/src/Api -f net10.0 --use-controllers --no-openapi false
dotnet sln backend/Opus127.Dengue.sln add backend/src/Api/Opus127.Dengue.Api.csproj
```

- [x] Remover o WeatherForecast de template (`WeatherForecast.cs`, `WeatherForecastController.cs`) se existirem.
- [x] `docker-compose.yml` na raiz:

```yaml
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Opus127_Dev!"
    ports:
      - "1433:1433"
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Opus127_Dev!" -C -Q "SELECT 1" || /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P "Opus127_Dev!" -Q "SELECT 1"
      interval: 10s
      retries: 10
      start_period: 20s
```

- [x] `backend/src/Api/appsettings.json`:

```json
{
  "ConnectionStrings": {
    "Dengue": "Server=localhost,1433;Database=DengueAlerts;User Id=sa;Password=Opus127_Dev!;TrustServerCertificate=True"
  },
  "AlertaDengue": {
    "BaseUrl": "https://info.dengue.mat.br/",
    "Geocode": "3106200"
  },
  "Cors": {
    "FrontendOrigin": "http://localhost:5173"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
```

- [x] `launchSettings.json`: perfil `http` em `http://localhost:5080`; Swagger mapeado. Sem perfil HTTPS obrigatório.
- [x] Rodar `docker compose up -d`, esperar o container `sqlserver` healthy. Rodar `dotnet build backend/Opus127.Dengue.sln`, esperar `0 Erro(s)` / `0 Error(s)`.

---

### Task 2: Semana epidemiológica (domínio puro)

**Files:**
- Create: `backend/src/Api/Epidemiology/EpidemiologicalWeek.cs`
- Create: `backend/src/Api/Epidemiology/EpidemiologicalCalendar.cs`
- Create: `backend/tests/Api.Tests/Opus127.Dengue.Api.Tests.csproj`
- Create: `backend/tests/Api.Tests/EpidemiologicalCalendarTests.cs`

- [x] Criar o projeto de testes e referenciar a API:

```powershell
dotnet new xunit -n Opus127.Dengue.Api.Tests -o backend/tests/Api.Tests -f net10.0
dotnet sln backend/Opus127.Dengue.sln add backend/tests/Api.Tests/Opus127.Dengue.Api.Tests.csproj
dotnet add backend/tests/Api.Tests/Opus127.Dengue.Api.Tests.csproj reference backend/src/Api/Opus127.Dengue.Api.csproj
dotnet add backend/tests/Api.Tests package FluentAssertions
dotnet add backend/tests/Api.Tests package NSubstitute
```

- [x] `EpidemiologicalWeek`:

```csharp
namespace Opus127.Dengue.Api.Epidemiology;

public readonly record struct EpidemiologicalWeek(int Year, int Week)
{
    public string ToContractString() => $"{Year}-{Week:D2}";

    public static EpidemiologicalWeek FromSe(int se)
    {
        var year = se / 100;
        var week = se % 100;
        return new EpidemiologicalWeek(year, week);
    }

    public int ToSe() => Year * 100 + Week;
}
```

- [x] `EpidemiologicalCalendar` (semana começa **domingo**; semana 1 contém **4 de janeiro**):

```csharp
namespace Opus127.Dengue.Api.Epidemiology;

public static class EpidemiologicalCalendar
{
    public static EpidemiologicalWeek FromDate(DateOnly date)
    {
        var jan4 = new DateOnly(date.Year, 1, 4);
        var week1Sunday = StartOfWeekSunday(jan4);
        if (date < week1Sunday)
            return FromDate(new DateOnly(date.Year - 1, 12, 31));

        var sunday = StartOfWeekSunday(date);
        var week = ((sunday.DayNumber - week1Sunday.DayNumber) / 7) + 1;
        return new EpidemiologicalWeek(date.Year, week);
    }

    public static DateOnly StartOfWeekSunday(DateOnly date)
    {
        var offset = (int)date.DayOfWeek; // Sunday = 0
        return date.AddDays(-offset);
    }

    public static IReadOnlyList<(int EyStart, int EwStart, int EyEnd, int EwEnd)> RangesForLastMonths(
        DateTimeOffset now, int months = 6)
    {
        var end = DateOnly.FromDateTime(now.UtcDateTime);
        var start = end.AddMonths(-months);
        var startWeek = FromDate(start);
        var endWeek = FromDate(end);
        if (startWeek.Year == endWeek.Year)
            return [(startWeek.Year, startWeek.Week, endWeek.Year, endWeek.Week)];

        return
        [
            (startWeek.Year, startWeek.Week, startWeek.Year, 53),
            (endWeek.Year, 1, endWeek.Year, endWeek.Week)
        ];
    }

    public static IReadOnlyList<EpidemiologicalWeek> LastWeeks(DateTimeOffset now, int count = 3)
    {
        var current = FromDate(DateOnly.FromDateTime(now.UtcDateTime));
        var list = new List<EpidemiologicalWeek>(count);
        var cursorSunday = StartOfWeekSunday(DateOnly.FromDateTime(now.UtcDateTime));
        for (var i = 0; i < count; i++)
        {
            list.Add(FromDate(cursorSunday));
            cursorSunday = cursorSunday.AddDays(-7);
        }
        return list;
    }
}
```

- [x] Testes mínimos em `EpidemiologicalCalendarTests`: `FromSe(202602)` → year 2026 week 2 e `"2026-02"`; `FromDate(new DateOnly(2026, 1, 4))` → `2026-01` (domingo conhecido da Infodengue); `RangesForLastMonths` em `2026-01-15` gera **duas** faixas (ano anterior + 2026).
- [x] Rodar `dotnet test backend/tests/Api.Tests/Opus127.Dengue.Api.Tests.csproj`, esperar todos Passed.

---

### Task 3: Entidade, DbContext, pacotes EF e migration

**Files:**
- Create: `backend/src/Api/Entities/DengueWeeklyAlert.cs`
- Create: `backend/src/Api/Data/DengueDbContext.cs`
- Modify: `backend/src/Api/Opus127.Dengue.Api.csproj` (packages)
- Modify: `backend/src/Api/Program.cs`
- Create: `backend/src/Api/Migrations/*` (geradas)

- [x] Pacotes na API:

```powershell
dotnet add backend/src/Api/Opus127.Dengue.Api.csproj package Microsoft.EntityFrameworkCore.SqlServer
dotnet add backend/src/Api/Opus127.Dengue.Api.csproj package Microsoft.EntityFrameworkCore.Design
dotnet add backend/src/Api/Opus127.Dengue.Api.csproj package Microsoft.Extensions.Http
```

- [x] Entidade:

```csharp
namespace Opus127.Dengue.Api.Entities;

public sealed class DengueWeeklyAlert
{
    public int Id { get; set; }
    public int EpidemiologicalYear { get; set; }
    public int EpidemiologicalWeek { get; set; }
    public decimal EstimatedCases { get; set; }
    public int NotifiedCases { get; set; }
    public int AlertLevel { get; set; }
    public string Geocode { get; set; } = "3106200";
    public DateTimeOffset SyncedAt { get; set; }
}
```

- [x] `DengueDbContext` com `ToTable("DengueWeeklyAlerts")` e unique index `(EpidemiologicalYear, EpidemiologicalWeek)`.
- [x] Em `Program.cs`: `builder.Services.AddDbContext<DengueDbContext>(o => o.UseSqlServer(builder.Configuration.GetConnectionString("Dengue")));` e após `Build()`, `db.Database.Migrate();` dentro de um using/scope (falha de banco deve logar de forma clara).
- [x] Gerar migration:

```powershell
dotnet tool install --global dotnet-ef
dotnet ef migrations add InitialDengue --project backend/src/Api/Opus127.Dengue.Api.csproj
```

- [x] Com Compose no ar: `dotnet ef database update --project backend/src/Api/Opus127.Dengue.Api.csproj`. Esperar `Done.` e tabela `DengueWeeklyAlerts`.

---

### Task 4: Cliente AlertaDengue + DTOs de origem

**Files:**
- Create: `backend/src/Api/Integrations/AlertaDengue/AlertaDengueWeekDto.cs`
- Create: `backend/src/Api/Integrations/AlertaDengue/IAlertaDengueClient.cs`
- Create: `backend/src/Api/Integrations/AlertaDengue/AlertaDengueClient.cs`
- Modify: `backend/src/Api/Program.cs`

- [x] DTO com nomes da origem (case-insensitive JSON):

```csharp
namespace Opus127.Dengue.Api.Integrations.AlertaDengue;

public sealed class AlertaDengueWeekDto
{
    public int SE { get; set; }
    public decimal casos_est { get; set; }
    public int casos { get; set; }
    public int nivel { get; set; }
}
```

- [x] `IAlertaDengueClient`:

```csharp
Task<IReadOnlyList<AlertaDengueWeekDto>> GetCityAlertsAsync(
    int ewStart, int ewEnd, int eyStart, int eyEnd, CancellationToken cancellationToken);
```

- [x] `AlertaDengueClient` usa `HttpClient` com `BaseAddress` de config, path `api/alertcity`, query `geocode={config}&disease=dengue&format=json&ew_start=&ew_end=&ey_start=&ey_end=`. `GetFromJsonAsync<List<AlertaDengueWeekDto>>`. Lista nula → lista vazia. HTTP não-sucesso → lançar `HttpRequestException`.
- [x] Registrar: `builder.Services.AddHttpClient<IAlertaDengueClient, AlertaDengueClient>(c => c.BaseAddress = new Uri(builder.Configuration["AlertaDengue:BaseUrl"]!));`

---

### Task 5: Repository + Sync service

**Files:**
- Create: `backend/src/Api/Repositories/IDengueAlertRepository.cs`
- Create: `backend/src/Api/Repositories/DengueAlertRepository.cs`
- Create: `backend/src/Api/Services/IDengueSyncService.cs`
- Create: `backend/src/Api/Services/DengueSyncService.cs`
- Create: `backend/src/Api/Services/DengueSyncHostedService.cs`
- Create: `backend/tests/Api.Tests/DengueSyncServiceTests.cs`
- Modify: `backend/src/Api/Program.cs`

- [ ] Repository: `GetAsync(int year, int week, ct)` → entidade ou null; `UpsertRangeAsync(IEnumerable<DengueWeeklyAlert>, ct)` — para cada item, se existir `(year,week)` atualiza casos/nível/`SyncedAt`; senão `Add`. `SaveChangesAsync` uma vez.
- [ ] `DengueSyncService`:
  1. `ranges = EpidemiologicalCalendar.RangesForLastMonths(DateTimeOffset.UtcNow, 6)`
  2. Para cada range, `GetCityAlertsAsync`
  3. Mapear DTO → entidade (`FromSe(dto.SE)`, `EstimatedCases = dto.casos_est`, etc., `Geocode` da config, `SyncedAt = now`)
  4. `UpsertRangeAsync`
  5. Retornar `int` quantidade upserted
- [ ] Catch de `HttpRequestException` / `TaskCanceledException` no hosted service: `ILogger.LogError`, não relançar.
- [ ] Hosted service: `ExecuteAsync` chama `IDengueSyncService.SyncLastSixMonthsAsync` uma vez (não loop).
- [ ] DI: repository scoped, sync service scoped, hosted service singleton que cria scope (`IServiceScopeFactory`) para resolver o sync scoped.
- [ ] Teste unitário: client fake devolve uma semana `SE=202340`, `casos_est=45`, `casos=38`, `nivel=2`; repository fake captura upsert; segunda chamada com os mesmos `SE` e `casos=40` resulta em update na fake. Terceiro teste: client lança; o service **propaga** (o hosted service é quem engole — testar o hosted com logger, ou documentar que o catch está no hosted e o service falha limpo). Manter catch **só** no `DengueSyncHostedService` e no controller de POST (500 vs 502). Service unitário espera exceção do client.
- [ ] `dotnet test`, esperar Passed.

---

### Task 6: Contratos HTTP, controller, CORS

**Files:**
- Create: `backend/src/Api/Contracts/DengueWeekResponse.cs`
- Create: `backend/src/Api/Contracts/SyncResponse.cs`
- Create: `backend/src/Api/Services/IDengueQueryService.cs`
- Create: `backend/src/Api/Services/DengueQueryService.cs`
- Create: `backend/src/Api/Controllers/DengueController.cs`
- Modify: `backend/src/Api/Program.cs`

- [ ] Response (JSON snake_case via atributo):

```csharp
namespace Opus127.Dengue.Api.Contracts;

public sealed record DengueWeekResponse(
    [property: JsonPropertyName("semana_epidemiologica")] string SemanaEpidemiologica,
    [property: JsonPropertyName("casos_est")] decimal CasosEst,
    [property: JsonPropertyName("casos_notificados")] int CasosNotificados,
    [property: JsonPropertyName("nivel_alerta")] int NivelAlerta);
```

- [ ] `DengueQueryService.GetByWeekAsync(int ew, int ey, ct)`: validar `ew` 1–53, `ey` 2000–2100; repository get; null → null; senão mapear `ToContractString()`.
- [ ] Controller `[ApiController] [Route("api/dengue")]`:
  - `GET` `[HttpGet]` params `ew`, `ey` → 400 se inválido, 404 se null, 200 com `DengueWeekResponse`
  - `POST` `[HttpPost("sync")]` → chama sync, 200 `{ "upserted": n }`; se `HttpRequestException`, 502
- [ ] CORS: `WithOrigins(config["Cors:FrontendOrigin"]).AllowAnyHeader().AllowAnyMethod()`.
- [ ] `Program.cs`: `AddControllers()`, `MapControllers()`, `UseCors`.
- [ ] Rodar `dotnet run --project backend/src/Api`. Esperar listening em `http://localhost:5xxx` (conferir `launchSettings.json`; fixar `http://localhost:5080` e `https://localhost:7080`).
- [ ] Com banco migrado e (se sync ok) dados: `curl "http://localhost:5080/api/dengue?ew=1&ey=2026"` deve ser 200 JSON ou 404 se aquela semana não veio. `curl -X POST http://localhost:5080/api/dengue/sync` deve ser 200 ou 502.

---

### Task 7: Testes de integração (SQL Server real)

**Files:**
- Create: `backend/tests/Api.Tests/Integration/DengueApiFactory.cs`
- Create: `backend/tests/Api.Tests/Integration/DengueGetTests.cs`
- Modify: `backend/tests/Api.Tests/Opus127.Dengue.Api.Tests.csproj`

- [ ] Pacotes:

```powershell
dotnet add backend/tests/Api.Tests package Microsoft.AspNetCore.Mvc.Testing
dotnet add backend/tests/Api.Tests package Testcontainers.MsSql
dotnet add backend/tests/Api.Tests package Microsoft.EntityFrameworkCore.SqlServer
```

- [ ] `DengueApiFactory` : `WebApplicationFactory<Program>` — para isso, adicionar em `backend/src/Api/Opus127.Dengue.Api.csproj`: `<InternalsVisibleTo Include="Opus127.Dengue.Api.Tests" />` **ou** tornar `Program` público via `public partial class Program { }` no fim de `Program.cs`.
- [ ] Factory sobe `MsSqlContainer`, troca connection string, **não** dispara sync real: substituir `IAlertaDengueClient` por stub NSubstitute / fake que devolve lista vazia no hosted service. Aplicar `Migrate()`.
- [ ] `DengueGetTests`: seed uma `DengueWeeklyAlert` year=2023 week=40 estimated=45 notified=38 level=2; `GET /api/dengue?ew=40&ey=2023` espera 200 e JSON com as quatro chaves do PDF; `GET /api/dengue?ew=1&ey=1999` 400; `GET` semana inexistente 404.
- [ ] Rodar `dotnet test backend/Opus127.Dengue.sln`. Esperar Passed. Requer Docker.

---

### Task 8: SPA Vite + Tailwind + três GET

**Files:**
- Create: `frontend/*` (scaffold Vite)
- Create: `frontend/src/lib/epidemiologicalWeek.ts`
- Create: `frontend/src/lib/api.ts`
- Create: `frontend/src/App.tsx`
- Create: `frontend/.env.development`
- Modify: `backend/src/Api/Program.cs` (CORS já na Task 6)

- [ ] Scaffold:

```powershell
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
npm install -D tailwindcss @tailwindcss/vite
npm install recharts
```

- [ ] `vite.config.ts`: plugin `@tailwindcss/vite`. `src/index.css`: `@import "tailwindcss";`
- [ ] `.env.development`: `VITE_API_URL=http://localhost:5080`
- [ ] `epidemiologicalWeek.ts`: portar a mesma regra Sunday/CDC (semana 1 contém 4 jan) e `lastWeeks(now, 3)` devolvendo `{ year, week }[]`.
- [ ] `api.ts`: `fetch(\`${import.meta.env.VITE_API_URL}/api/dengue?ew=${week}&ey=${year}\`)`; 404 → `null`; outro erro → throw.
- [ ] `App.tsx`: `useEffect` dispara `Promise.all` dos **três** fetches (não um único agregado). Estados: loading, erro de rede, lista de `{ week, data | null }`.
- [ ] UI: header “Dengue — Belo Horizonte”; 3 cards (cores: 1 verde, 2 amarelo, 3 laranja, 4 vermelho); tabela com colunas Semana / Estimados / Notificados / Nível; `BarChart` Recharts com duas séries `casos_est` e `casos_notificados`. Semana sem dado: card “Sem registro”.
- [ ] Rodar API na 5080 e `npm run dev` em `frontend`. Abrir `http://localhost:5173`. Confirmar três requests no DevTools Network para `/api/dengue?ew=&ey=`.

---

### Task 9: README de entrega

**Files:**
- Modify: `README.md`

- [ ] Substituir o stub por README em português com:
  - Pré-requisitos: Docker, .NET 10 SDK, Node 20+
  - `docker compose up -d`
  - `dotnet ef database update --project backend/src/Api/Opus127.Dengue.Api.csproj` (ou só `dotnet run`, se `Migrate()` no boot)
  - `dotnet run --project backend/src/Api` → `http://localhost:5080`
  - `cd frontend && npm install && npm run dev` → `http://localhost:5173`
  - Sync automático no startup e `POST /api/dengue/sync`
  - Exemplo de GET
  - `dotnet test backend/Opus127.Dengue.sln` (Docker necessário para integração)
  - Link para `docs/prd.md` e `docs/adr/`
- [ ] Conferir que senha `Opus127_Dev!` no README é a mesma do Compose (dev only).

---

## Coverage check

| Requisito | Task |
|---|---|
| Q1 ingestão 6 meses BH | 4–5 |
| Virada de ano / duas faixas | 2, 5 |
| Upsert | 5 |
| Startup + POST sync | 5–6 |
| Q2 GET JSON + 404 | 6–7 |
| Camadas Controller/Service/Repository | 5–6 |
| SQL Server ORM | 3 |
| Q3 três GET + cards/tabela/gráfico | 8 |
| Testes unitários | 2, 5 |
| Testes integração SQL real | 7 |
| README | 9 |
| .NET 10 / React TS Tailwind | 1, 8 |

Nenhum item do PRD ficou sem task. Frontend automatizado continua fora de escopo.
